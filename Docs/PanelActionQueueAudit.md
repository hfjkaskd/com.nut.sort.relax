# Panel action queue and hide callback

OriginalPanelActionQueue restores AddActionQueue (0x9F8808) and DequeueActionQueue (0x9F8860): enqueue only, one FIFO entry per explicit dequeue, removal before invocation, null entries consumed without calling, no automatic drain or reentrancy suppression. Exceptions propagate after the throwing entry was removed, leaving later entries intact.

OriginalFailurePanelView now exposes BindHide/Hide for the source BaseLSSPanel.HideLssPanel (0x9C5218). It calls the optional hidden callback first, then schedules actionQueue.Dequeue with serialized queueDelay 2.5. Source callback 0x9C5464 invokes the manager's dequeue. The delegate references the queue, not a live view requirement; a scene-owned ScheduleDelay coroutine survives panel destruction. Repeated Hide calls are not suppressed, and a throwing hidden callback prevents scheduling. The existing registry integration now invokes this real Hide method before test destruction rather than an empty hide adapter.

61 regression PASS markers in Library/unity-panel-queue-validation.log. Updated Play validation passes in Library/unity-panel-queue-play.log: actual Restart Button -> scene reset -> Hide -> Destroy, followed by one deferred queue action after the panel is destroyed. The fixture waited beyond the configured delay; it does not establish sub-frame timing precision. Latest normal/gray screenshots were regenerated and the gray capture inspected. No compiler errors or exceptions found; preference fixture restored with backup absent. Tests additionally cover FIFO/null/reentrant/exception cases, callback-before-schedule, live queue content, repeated Hide and destruction-independent dispatch.

The previous PanelRegistryAudit describes the preceding checkpoint; its empty hide adapter has now been replaced. Normal startup still needs unified panel-manager ownership/binding and actual queued event producers; the new queue is not automatically populated with guessed events. SDK treatment remains unchanged. Complete lifecycle/AB/GM parity is still incomplete.
