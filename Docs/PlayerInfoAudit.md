# PlayerInfo restoration and withdrawal refresh tail

PlayerInfo.Init (0x9b7068–0x9b7570) first sets localPosition to zero, then hides its root. ServerConfigData.LSS260820 exits while hidden. The old-mode branch localizes child labels before checking the shared PMD response. Missing response keeps the view hidden. With response present it writes text 31 with GetTimeFormat(empty, "d") (null string becomes empty), PassLevelPeopleCount, challenge count and formatted gold. Levels <=2 show challenge count "1" and use bear_list[0].psi_value only for level 1, otherwise bear_list[1].psi_value. Levels >2 use TodayChallengeTimes and PMD TXRandomValue without reading reward target data. It activates the root, then creates/initializes PMDSimple under PMDPos with the supplied level; each invocation requests another child, without clearing old children.

OriginalPlayerInfo retains this order and binds server/time/random/PMDSimple consumers explicitly. It does not manufacture absent PMD data. Date, people count and PMD random selection providers still need production time/response composition, and PMDSimple view restoration is outstanding.

Recovered Resources/prefabs/playerinfo/PlayerInfo.prefab preserves all 13 GameObjects, transforms, original Image/TMP values, sprites, font assets and material references. Exported script references are replaced by official Unity UI/TMP and the new runtime component. Three localized static labels (32–34) are configured in the prefab. Added source sprite dependencies and missing white-outline material preserve their original GUIDs, with the material using the existing matching official TMP shader. No runtime hierarchy or style construction is introduced.

TXPanel.Refresh tail at 0x9cd490–0x9cd57c instantiates PlayerInfo, calls Init with captured _level, resets static IsPlayGoldTween to false, then reads live IsGuideGold and conditionally replaces Tip with text 159. OriginalWithdrawalPanelFlow.FinishRefresh preserves these operation and failure boundaries. The regression invokes the actual recovered PlayerInfo through this tail. The complete TXPanel owner still needs to supply prefab instantiation and its actual static flag/TMP bindings.

Validation uses the actual prefab, verifies 13 RectTransforms, no missing scripts, resolved sprites/fonts/materials, and exercises mode/data gates, early/later levels, repeated child initialization, null date, missing configuration and child-init failure. Test data, clock/count/random values and PMDSimple callback are explicit fixtures. SDK handling is unchanged. No new screenshot or full-view/game parity claim is made.

Verification: source/target block comparison found 37 non-script blocks byte-identical after newline normalization, 49 blocks total; all Image/TMP blocks differ only by official script remapping. Unity 2022.3.62f3 full regression passed 119 markers in Library/player-info-tail-validation.log, including NUT_PLAYER_INFO_VALIDATION_PASS and final NUT_CONTENT_VALIDATION_PASS. No compiler errors or exception entries. Preference backup removed after restoration, launched Unity process exited. Existing pooled-object reparent shutdown warnings remain.
