# Complete failure panel prefab and view

FailPanel.prefab restores all 18 original objects. Every RectTransform block is identical to the source full prefab. The existing CoinTip punch and five sparkle color components are composed with original parents/order; original sprites, fonts/materials and active states are retained. CoinTip is inactive in the source and remains inactive. There is no child named main, so the base optional panel scale tween is absent. No runtime static UI creation is introduced.

OriginalFailurePanelView restores InitLssPanel 0x9D6964: base-style localized labels, code-bound Restart/Revive Button listeners, then GameLose audio. Refresh uses the restored failure data flow and applies gray to active Image descendants of Revive, without disabling the Button. Clicks use the common gate and action-before-Click-audio order. Restart accepts the existing scene RestartAfterFailure action and closes afterward. Revive clears failure before invoking the supplied existing SDK boundary; no success callback/reward is fabricated. Reinitialization replaces listeners. Visual pointer feedback is attached to the same standard Button objects in the prefab.

Validation: 59 regression PASS markers in Library/unity-failure-panel-validation.log; Play-mode PASS in Library/unity-failure-panel-play.log. No compiler errors or exceptions found; preference backup restored/absent. Unit/integration checks cover full hierarchy, missing scripts, localized labels, raw amount/gray refresh, click gating, action/audio order and listener replacement. The actual scene Play fixture rendered and captured both current normal and gray layouts at 480x1040; both images were inspected. Its real Restart Button invoked scene reset before the close callback. Fixture reward/config data are explicit test inputs, not production responses. Latest captures: Library/ValidationCaptures/failure-panel-play.png and failure-panel-gray-play.png.

Normal startup panel routing remains unbound to the new complete failure panel, and revive SDK handling stays outside this view unchanged. Device visual equality, complete lifecycle routing, other tools and AB/GM completion remain unproven/incomplete. This checkpoint establishes the full prefab/view and actual scene fixture, not whole-game 1:1 completion.
