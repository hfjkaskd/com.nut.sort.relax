# Success guide through actual target completion

The SuccessGuideHost Play fixture now opens the actual UIName 35 TXGuideTargetCompletePanel host instead of ending at a dispatcher counter. Its real Continue Button runs OriginalGuideTargetCompletion, starts the close tween, sets IsCompleteRecordGuide, calls the initialization boundary with (true,false), then invokes the retained global guide callback. That callback reaches OriginalSceneGuideUI.TargetGoldInfo(false) before incrementing GuideIndex from one to two and saving through the real scene/user store. The fixture verifies the displayed first-stage gold and deserialized saved progression. Initialization and network response handling remain explicit fixture boundaries, not fabricated production completions.

The extended chain exposed a stale-close bug: the retained guide callback called Close on an already destroyed view, which unconditionally dispatched its old registry Hide(7) closure. This emitted `panel not exist :NewbieGuidePanel` and could target a different live registration if one had been opened in the interim. Native BaseLSSPanel.CloseLssPanel 0x9c4f78 checks the owned Unity object before panel lookup (0x9c5004-0x9c5048). OriginalNewbieGuideView.Close now returns when its Unity object is destroyed, preserving live close behavior and preventing stale registry dispatch. The request, guide increment/save and successful global callback clearing remain in their original order.

A regression destroys the actual guide prefab and calls its retained Close method, asserting that the prior close consumer is not invoked again. The actual-scene chain verifies that request and persistence still complete and that the target panel is removed after its close animation. No Editor-specific runtime path, reflection, SDK integration or generated UI was added.

Scope remains incomplete: the initialization consumer and target reward request/response composition are not yet wired through production startup. The test does not establish the subsequent withdrawal UI lifecycle or all region variants. No visual asset changes in this round.

Verification: actual-chain Play passed in Library/success-guide-target-chain-play-2.log without the prior missing-guide diagnostic. Full regression passed 110 markers and final NUT_CONTENT_VALIDATION_PASS in Library/success-guide-target-chain-regression-2.log. No C# compiler errors or exception entries in the successful logs. The old target-return test expectation was corrected to retain request/save but omit a destroyed view's stale close dispatch. Preference fixture restored with no backup remaining.
