# Common Button gate and counted mask integration

OriginalButtonGate restores the pre-action portion of AddLSSListener callback 0x9C04A8. It reads the live IsInitDone provider and returns false if initialization is incomplete. Otherwise it acquires OriginalCountedMask using serialized OriginalPanels.ClickMaskDuration, then returns true to the existing view action/audio sequence. The source literal at ELF address 0x2017858 was read as float 0.20000000298, matching the configuration's 0.2. No mask-count, modal-state or duplicate-click predicate is added: source UI raycast blocking is separate from direct callback invocation.

64 regression PASS markers in Library/unity-button-gate-validation.log; no compiler errors or exceptions found, preference fixture restored with backup absent. The new test instantiates actual FailPanel and binds its standard Restart Button to the gate. It verifies initialization rejection, mask activation/scheduling before action, close/audio order, repeated direct invocation acquiring independent masks, live initialization changes, and preservation of delayed release after action exceptions. The test uses an injected initialization flag and scheduler; it does not establish source initialization lifecycle or actual pointer raycast behavior in normal startup.

Normal startup and replay controller still await unified binding to the real IsInitDone owner and native mask object. No premature readiness approximation was introduced. SDK vibration remains excluded/unchanged. No visual assets changed. Full lifecycle, region AB and GM parity remain incomplete.
