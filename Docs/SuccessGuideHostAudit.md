# Actual success-to-guide target binding

OriginalSuccessPanelHost.GetSuccessGuideTarget resolves registered UIName 9 at guide presentation time and returns its actual MoreButton plus that same panel instance's GetCallback. This preserves the recovered distinction: the visual target is MoreGetBtn, but the guide operation is virtual GetCallback, not MoreGetCallback and not Button.onClick.Invoke. The existing OriginalGuideBranchAdapter consumes this binding unchanged. Missing registration fails instead of supplying a placeholder target.

The existing NewbieSuccessGuide regression now instantiates SuccessPanel and uses its actual MoreButton, replacing the TXGuideTargetCompletePanel stand-in. Its existing placement, sizing, gate, callback ordering, overflow, failure and delayed reopen checks remain intact.

New actual-scene Play validation composes OriginalSuccessPanelHost, OriginalNewbieGuideHost, OriginalSceneGuideUI, OriginalGuideBranchAdapter and OriginalCountedMask with the native scene scheduler. SuccessPanel opening completion creates guide panel 7 and resets GuideIndex to zero. The guide uses the real target's dimensions. Clicking its standard Continue Button calls the captured early-level SuccessPanel.GetCallback, advances GuideIndex, immediately destroys the guide, acquires the 1.5-second counted mask and schedules guide reopen after 0.5 seconds. The actual success close tween removes its registration before guide reopen. The reopened guide captures index one, installs its completion callback, closes and dispatches target panel 35 with level one. The mask releases independently later. No ad or reward request occurs in this chain.

The test's panel-35 dispatcher is an explicit boundary; it does not simulate a target-panel completion or server response. It restores global guide callback and the preference fixture. The mask consumer records activation rather than claiming a verified pointer-blocking scene mask. Production startup, unified all-panel composition, the subsequent withdrawal/target response chain and complete lifecycle parity remain unfinished. This round does not change visual assets.

Verification: Unity 2022.3.62f3 NUT_SUCCESS_GUIDE_HOST_PLAY_PASS in Library/success-guide-host-play.log, plus full NUT_CONTENT_VALIDATION_PASS in Library/success-guide-host-regression.log. Neither successful log contains C# compiler errors or exception entries. Existing exit-time pool-parent warnings remain outside this change.
