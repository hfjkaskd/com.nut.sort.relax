# PlayerGoldGetHint prefab and presentation

Recovered the current typed MainPanel subtree rooted at GameObject 1043919389829319: six objects, 23 serialized blocks, original RectTransforms, TMP settings, sprites, FireworksEffect particle modules and renderer ordering. Replaced only unavailable script references and exposed native motion constants as serialized prefab settings. Restored the missing background and confetti dependencies; retained existing matching dependencies and official Unity components. Runtime code creates no static hierarchy.

OriginalPlayerGoldHintView connects the previously verified schedule, text and icon helpers. Init places the full local position at (0,500,0) and resets the schedule sentinel. Update polls the UTC schedule independently of scaled animation time. Bind accepts actual providers; it does not fabricate region, network or reward data. Successful push ordering is schedule NextPush, channel icon, generated name, live-level amount, entrance. Self display selects saved channel metadata before assigning self name, icon and amount. RefreshTip and Show(Action) only refresh the random icon.

Native evidence: Init 0x9DE850, Show 0x9DFFBC, its completion 0x9E0ABC, Show(Action) 0x9E057C with completions 0x9E0BC8/0x9E0CFC, RefreshTip 0x9E06C8, ShowSelf 0x9E06D4 with completion 0x9E0B40. Ordinary/self entrances move the full local vector to zero in 0.5 seconds linearly. On completion, ordinary display plays a non-null effect; self display requires the effect. Both create a new 0.5-second return to (0,500,0), delayed three seconds. No ease is specified on those returns: the current source DOTweenSettings.asset defaultEaseType=6 selects OutQuad. Callback display moves only Y to zero in 0.5 seconds with OutBack, then creates a Y-only linear return to 500, duration 0.5 and delay 0.5; its nullable callback fires after return completion. It does not play particles.

The existing scene-owned scaled animation driver runs these tracks even while the hint is inactive. Tracks capture their start on activation, keep current X/Z for Y-only movement, retain overlapping displays, and append completion-created tracks for the following update. There is no automatic cancellation on Init, Show or visibility changes. Small reusable struct lists avoid per-frame allocations; no third-party tween assembly is added.

Validation: 43 full Unity regression PASS markers, including original hierarchy/dependencies, schedule-to-presentation ordering, all three entrance/hold/exit paths, default/explicit easing, particles, Y-only axis preservation and repeated callbacks. Actual Play validation additionally passed UTC polling at timeScale zero, scaled movement pause, real entrance, inactive-owner return, callback timing and unchanged user save. Preference backup restored and removed. Logs: Library/unity-player-hint-view-validation.log and Library/unity-player-hint-play-validation.log. Current Play capture Library/ValidationCaptures/player-gold-hint-play.png was visually inspected for layout, font/icon and confetti rendering; it uses explicit test-only US/amount fixtures and is not evidence of a real withdrawal.

Production Top assembly/provider binding, full lifecycle/region branches and device visual parity remain pending. This restores the component and verifies it in the real scene through an Editor test fixture; it does not claim complete 1:1 game parity or add SDK behavior. Separate authored motion execution still needs broader cross-component ordering parity checks.
