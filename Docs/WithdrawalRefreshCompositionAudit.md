# Withdrawal refresh composition

OriginalWithdrawalRefreshFlow now composes the recovered TXPanel.Refresh body in native order: captured-level header; pending ComeOnGold application/clear/save/display tween; TXText reset to text 1 (0x9cc134–0x9cc198); GetTXStage using the now-current balance (0x9cc19c–0x9cc1cc); early, third or later progress branch; shared PlayerInfo initialization, IsPlayGoldTween reset and live IsGuideGold final Tip override. It preserves branch-local table reads through providers and does not eagerly evaluate unused stages. One progress instance retains the original task-complete flag.

Regression uses the actual OriginalRewardProgress stage evaluator, actual pending-gold tween prefab and actual PlayerInfo prefab. A fixture balance increase from 5 to target 10 must select the login-days branch after credit, producing 1/3 instead of stale amount progress. The full trace asserts header/apply/save/animate/default-caption/HUD/progress/later-caption/PlayerInfo/reset/final-tip ordering. Advancing the actual tween after static flag reset updates only the displayed gold. An early captured-level branch succeeds without reward target data and retains default caption 1. A save failure preserves applied/cleared pending state and aborts caption/stage/PlayerInfo/reset work. These are explicit fixture balance/save/time/view consumers, not simulated production SDK success.

The complete TXPanel prefab/controller/host remains pending. Source prefab inspection additionally identified ProgressTip DOTweenAnimation type 10 as PunchRotation, duration 1, z punch 10, vibrato 10, elasticity 1 and infinite yoyo loops. It must be restored from the original punch algorithm before presenting the full view as visually equivalent; substituting a constant rotation loop would differ. No screenshots or full-game parity claims are made this round. SDK handling remains unchanged.

Verification: Unity 2022.3.62f3 full regression passed 121 markers in Library/withdrawal-refresh-validation.log, including NUT_WITHDRAWAL_REFRESH_VALIDATION_PASS and final NUT_CONTENT_VALIDATION_PASS. No compiler errors or exception entries. Preference fixture restored and backup removed; launched Unity process exited. Existing pooled-object reparent shutdown warnings remain.
