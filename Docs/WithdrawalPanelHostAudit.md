# Withdrawal panel registry host and guide chain

OriginalWithdrawalPanelHost owns UIName 18 using the existing native-order OriginalPanelRegistry. It instantiates the configured actual TXPanel prefab, registers it before binding services/Init, then calls Refresh. A per-instance service factory receives the registry close callback. Animated close invokes Hide, actual panel Hide schedules queue continuation, then the registry destroys/removes the entry. Refresh and explicit Hide delegate to the same entry. Duplicate Show retains the existing panel and emits the original panel-exist diagnostic. GetWithdrawalGuideTarget returns the actual registered TXPanel Withdraw Button and that same instance's GetCallback, rather than a substitute control.

The existing Success-guide Play chain now connects the held OriginalGuideTargetRequest response to this real host. It verifies actual SuccessPanel opening -> guide click/direct success close -> counted mask and delayed guide reopen -> actual target-completion Button -> initialization boundary/request -> persisted guide index 2 -> held response -> real registered TXPanel -> real opening tween -> actual withdrawal guide. The guide's target dimensions come from the actual Withdraw Button. The test then explicitly closes the guide fixture, exercises host Refresh (a new PlayerInfo child), duplicate Show, native online-time close hint, animated registry removal, and a clean new instance on reopen. This replaces the prior panel-18 dispatch-only probe.

Verification: Unity 2022.3.62f3 full regression passed 123 markers in Library/withdrawal-host-validation.log with final NUT_CONTENT_VALIDATION_PASS. Expanded NUT_SUCCESS_GUIDE_HOST_PLAY_PASS passed in Library/success-guide-withdrawal-host-play.log. No compiler errors or exception entries in either successful log. The Play log intentionally contains one panel exist :TXPanel from duplicate Show validation, and no missing-panel diagnostic. Preference fixtures restored and backup removed; both launched Unity processes exited. Existing pooled-object reparent shutdown warnings remain.

Production scene entry-point/service composition remains pending. The chain still explicitly supplies initialization/transport/withdrawal service fixtures; it does not imply a live server response or SDK success. It enters withdrawal guide index 2 but does not claim to complete its follow-on panel 36 flow; the test deliberately removes that guide before independently checking host close/reopen behavior. No new screenshot claim is made this round. SDK handling is unchanged.
