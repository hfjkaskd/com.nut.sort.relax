# Marquee item presentation recovery

Recovered PMDItem from the current MainPanel export: three RectTransforms and fourteen serialized objects, original background and pay icon, TMP font/material, auto-size range 18-30, disabled wrapping and horizontal preferred-size ContentSizeFitter. OriginalMarqueeItemView uses configured references and parameters; it does not construct static UI at runtime. The prefab is currently standalone.

Init follows the current ARM64 PMDItem.Init (0x9B914C): build text using the captured level, obtain the live country's channel, draw a channel, load Atlas/PaySimple/{channel}, assign only a non-null sprite, then schedule the width callback. It neither changes IsReady nor cancels earlier width callbacks. The callback reads the current Tip rect width, adds 140, and preserves the current item height (0x9B9B24). The binary constant at 0x2017854 is float 0.01. DelayCallback starts a WaitForSeconds coroutine on the main game owner; the view accepts that scheduling boundary explicitly, rather than silently starting it on the item. Production host binding remains required.

The icon's CircleImageMaterial uses UI/CircleImage, whose exported body was a dummy. An authored Unity shader restores the current GLES program's aspect-scaled radial distance, reversed cubic smooth edge, sampled color times vertex/tint color, and coverage applied only to alpha. Radius 0.49 and AA width 0.01 come from the original material. Source render state uses SrcAlpha/OneMinusSrcAlpha for both color and alpha, GUI depth mode, disabled depth writes/culling, and material stencil/color-mask properties. No new RectMask2D clipping variant is introduced. Raw shader/native programs remain local evidence and are not published.

37 complete Unity regression PASS markers in reverse-workspace reconstruction-nut/logs/unity-marquee-item-validation.log; no compiler or validation exceptions, preference backup absent after restoration. The item test checks native prefab references, font/fitter, shader/material, exact text-channel-icon-delay order, retained missing sprite, unchanged IsReady and two independent callbacks reading the latest width/height.

Remaining: PMDBullet launcher/pool/movement, actual main-owner coroutine binding, production Top lifecycle and original-device visual comparison. The view's injected scheduling test is not proof of an integrated coroutine or complete player flow. SDK behavior is unchanged.

Current Unity graphics capture passed (unity-marquee-item-capture.log): Library/ValidationCaptures/marquee-item.png was visually inspected for text, background and circular PayPal icon, with no missing/magenta material. Width is 1002.05 for the deterministic validation text. This editor rendering fixture is not live Play timing or original-device parity evidence.
