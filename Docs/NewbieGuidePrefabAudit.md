# Newbie guide prefab restoration

NewbieGuidePanel now preserves all 26 source RectTransform blocks exactly. Source sprite/font/material references were retained, with missing bg/bg1/hand assets restored. Scripts were mapped to official UGUI/TMP components, including the nested GraphicRaycaster and ContentSizeFitter, and authored runtime components. Original serialized Radius 25, six segments, target reference, realtime refresh and raycast flag are retained by OriginalHollowMaskGraphic.

OriginalNewbieGuideView binds the recovered SetTeach control flow to actual Teach, Hand, mask, four marker Images and tip-position references. Marker sprites use source dian/dian1 paths. Tip positioning targets its parent in world space; negative IDs hide that parent and null positions use the source logged fallback. The full ShowGuide routing and Button actions remain outstanding; the three standard Buttons are retained with no Inspector-persistent event injection.

Hand and arrow animations use serialized local-rotation/local-position targets with OutQuad restart loops (0.7/0.8 seconds), per original DOTweenAnimation enums. The Get marker reuses the recovered one-second yoyo PunchRotation curve. Native scaled-time animation ownership continues while the target is hidden. No third-party animation assembly was added.

Unity full regression checks all 26 transforms, missing scripts, three Buttons, font, progress sprites and mask/animation references. A direct source comparison found every RectTransform block unchanged. Actual Play verifies the Graphic produces its 40-vertex mesh, and renders teaching seeds zero and three through OriginalTeachingFlow. Current captures Library/ValidationCaptures/newbie-mask.png, newbie-teach-0.png and newbie-teach-3.png were inspected. Seed zero shows one green marker and the source move-nut text; seed three shows four green markers and the source withdrawal text. The mask capture deliberately precedes ShowGuide and retains original uninitialized placeholder text; it is not the final entry screen or evidence that startup is complete.

Complete ShowGuide state routing, click callbacks, placement targeting and production startup binding remain unimplemented. The fixture uses explicit teaching state and an operation-state callback; it does not claim gameplay input is yet connected to this guide. SDK treatment is unchanged.

Validation: 75 full regression PASS markers in Library/unity-newbie-prefab-final-validation.log; Play PASS in Library/unity-newbie-prefab-play.log; preferences restored. Final batch log contains an AndroidDeploymentTargetsExtension device-scan process-exited exception before validation, with no game-validation failure. Initial validation API compile error was corrected before successful runs.
