# 当前实现状态

当前核心场景背景已修复 Android 自动导入为 ETC_RGB4 的偏差，按源包配置 ASTC 10×10。源 PNG 不变，GPU 读回误差约降低 95%，当前默认玩法截图及浮动提示回归已通过；详见 BackgroundTextureFidelity.md。真机及整屏 1:1 尚未证明。

## 当前默认入口（2026-09-09）

默认本地玩法已接入原子回合点阵：按原表显示四/五点、当前点闪烁与离开子回合时隐藏。收益里程碑保持跳过；详见 [子回合点阵接入](LocalSubroundView.md)。

已补齐原表 SubTotalRound/SubRound 的中间子回合自动续局，最后子回合显示结算；普通 Round 不跳过结算；见 [本地多回合推进](LocalSubroundProgression.md)。

已接通用户批准的本地关卡推进、四段教学及连续下一关、失败重试、原版重开、棋盘/操作记录恢复，原版底栏撤销/交换/加螺杆操作，以及四类玩法解锁弹窗。内部关卡 4/17/57/107 使用原配置减四的判断；确认后本地持久化，重进不重复。无路可走的读档棋盘优先触发原失败延时。

首关现已接入原版教学面板、逐步提示、四个进度标记与操作权限切换；本地模式的收益文案替换为明确的本地推进提示。

特殊螺杆已补验默认存档入口、完成联动、读档、撤销后重载和继续操作；详见 [特殊螺杆集成验证](LocalSpecialScrewIntegration.md)。

失败重试现已改用原 FailPanel 的本地预制体变体，并恢复缺失的 GameLose 音频；SDK 收益/复活子树隐藏。详见 [原版失败接入](LocalNativeFailure.md)。

过关现已接入原版标题、入场/关闭动画和按钮预制体变体；关闭完成后才进入下一关。详见 [原版过关表现](LocalNativeSuccess.md)。

Android ARM64 IL2CPP 本地开发 APK 已构建，签名和包元数据校验通过；修复动态后备字体缺失图集，166 项内容回归通过。尚未连接真机验证。详见 [Android 本地构建](AndroidLocalBuild.md)。

撤销无历史和加螺杆上限已恢复原 TopCanvas 浮动提示，不再弹出阻断操作的本地卡片；详见 [原版浮动提示](LocalPopTips.md)。

详见 [教学接入](LocalGameplayTeaching.md)、[本地推进](LocalGameplayProgression.md)、[核心道具入口](LocalCoreTools.md)、[玩法解锁](LocalGameplayUnlocks.md)。SDK 收益、广告回调、提现与服务器奖励均未模拟；完整奖励引导、外围生命周期、国家/AB GM 和真机视觉对照仍未完成。

下方保留历轮恢复记录；其中早期“尚未接入”等描述反映当轮状态，当前入口以本节和上述最新验证文档为准。

## 已提交恢复点

初始恢复点 `6a99651f9cf74a1f900b2d93f8eff87945d31dad` 已推送到指定仓库 main，并校验远程一致。提交作者仅在此仓库配置为用户提供的身份。

## 已实现并验证

- 使用指定 Unity 2022.3.62f3 创建原生工程。
- 恢复两套原始加密索引与所有 1,474 份棋盘数据，资源路径和内容来自本包，不是生成关卡。
- 根据原 RVA 0x9B4718、0x9FBAE4、0x9FBC04、0x9FCE64 实现 Resources 路径加载、AES-CBC/PKCS7 解码及原关卡结构读取。
- 原主索引为 1–219 共 219 项；循环索引为 220–299 共 80 项。
- 数据路径和原解码参数放在 `Assets/Resources/Configuration/OriginalContent.asset`。运行时只保留已解码索引，棋盘按需求加载。
- Unity JsonUtility 会将缺省 BIM 读成非空对象，导致空槽语义变化。已改为 **Unity 官方** `com.unity.nuget.newtonsoft-json@3.2.1` 的显式 JToken 字段读取；不使用 JsonSerializer、ToObject、反射字段查找或枚举字符串 key。
- Unity 内验证通过：1,476 份资源的明文 SHA-256、1,474 份棋盘的螺杆/格子/占用数量和关卡标识、全部索引种子引用、首关 2 杆×4 槽与 4 枚颜色 11 螺母、无效资源/输入的明确失败行为。

- 已接入原 GetLevelData 的关卡选择规则：第二关开始 +3、主/循环索引切换、超出末关后的 [220,300) 随机、LSS260820/LSSSHSLV 特殊引导映射、种子越界后的持久随机标记。边界测试与关卡加载回归已通过。

- 已恢复世界空间布局配置与计算：6/11 杆分排阈值、每排容量向上取整、1.65/2.2 横向间距、6.6 排间距，以及原相机宽高比缩放和最小倍率。原始浮点字节与函数汇编随工程保存。边界坐标和相机验证通过；此模块尚未接入可玩场景。

- 已实现原版排序状态层：固定槽位、只选中顶端一枚、同色组搬移、容量截断、隐藏颜色阻断及移走后的揭示、误点回退、原完成判定。数据搬移与最后一枚到达/完成动画的操作门控分开。原始首关一步获胜及上述分支通过 Unity 验证。工具入口仅输出原分支请求，奖励/引导/工具消费及动画视图尚未接入。

- 已接入 Unity 官方 URP 14.0.12、UGUI 1.0.0、TextMeshPro 3.0.6，并保存实际依赖锁。版本来自本机编辑器官方模板及实际解析包；接入后四组回归通过。原版 URP 资产和材质随后已恢复并启用，场景画面对齐仍需验证。

- 已恢复并启用原 URP-Balanced 配置、渲染器、关闭的 SSAO 特性，导入 5 个原始网格、19 个原始材质及 3 张贴图。标准 Shader 引用映射到官方包，原材质颜色、光滑度、金属度、透明模式和网格顶点数量验证通过。两项旧运动矢量引用按官方渲染器相同字段迁移；画面对照未完成，不视为完全一致。

- 已恢复 12 色螺母、隐藏螺母和槽位 Prefab，接入原生显示组件与延迟加载对象池。隐藏/揭示、对象复用及槽位显示规则通过实例验证。灰度 Shader 的 12 组 GPU 像素测试通过，完整回归仍通过。原始提取证据保留本机；入场、选中、放回和摆动动画已随后实现；跨杆搬移已随后实现；完成特效和主场景绑定仍未完成。

- 已在原螺母 Prefab 接入原生入场、抬起、放回和选中摆动动画，参数及缓动曲线由配置资源驱动。原生 Transform 采样验证覆盖关键位置、旋转方向、延迟、回调顺序、中断及池化释放，完整回归通过。落点火花实际播放、整杆完成动画和主场景绑定仍待接入，尚未完成动态画面对照。

- 已实现跨杆搬移动画：立即重绑目标槽位、非缩放时间错峰、已选中直接横移/未选中先抬起、保持世界坐标换父级、横移后落下及完成回调。使用原始首关排序记录和真实螺母 Prefab 验证，覆盖暂停时延迟、目标杆抬起高度、释放取消及整杆完成门控。完整八组回归通过；此结果仍是组件验证，主场景与完成特效尚未接入。

- 已恢复原 Screw Prefab 的杆体、杆帽、广告锁定两套结构、遮罩精灵和原生粒子组件，接入槽位 Prefab 池及状态刷新。保留根缩放 1.4、固定碰撞体/杆帽高度和 InitPos/ReadyPos 共用节点的覆盖顺序；LSSAB 控制两套锁定外观。真实 Prefab 验证覆盖短杆提示、完成帽、遮罩颜色和释放复用，九组回归通过。三处原 Spine 组件及其空网格渲染组件未导入，原目标节点保留，动画请求已表达但尚无原生动画资产消费；不能视为遮罩/石块/隐藏覆盖动画已复刻。粒子画面和完整场景仍待最新实机对照。

- 已恢复原 Level/ScrewPosGroup 静态层级，并用配置好的位置 Prefab 驱动原布局；实际棋盘已组合原生杆体、固定槽位和按颜色加载的螺母。保留 0.5/1.5 秒缩放时间初始化延迟，选中/放回/跨杆动画由真实排序结果驱动。整杆完成帽按 OutCirc 升起放大、峰值刷新、InCirc 回落，结束后释放操作门控。原始首关在实际 Prefab 链路上从入场到一步完成、双延迟路径和换关复用通过验证，十组回归通过。启动 Scene 与用户输入随后已接入，奖励/引导/存档/声音/火花等消费者及最新完整画面对照仍待接入。

- 已接入 `Assets/Scenes/LuoSiSortGame.unity` 原生运行入口，并配置为构建首场景。恢复原三台相机/堆叠关系、灯光、世界背景、光照数据和反射立方体；鼠标/单指释放使用统一运行时逻辑通过相机射线操作游戏杆体。当前从显式配置加载第一关，尚未连接原存档/服务器/地区初始化。十一组回归及独立 Play 模式验证通过：实际 Start、Update、选中/搬移和完成门控正常。
- GPU 实测发现旧 Unlit/Texture 背景没有桌面 URP 深度预通道，产生黑底及未覆盖旧像素。背景静态材质已迁移到 Unity 官方 URP Unlit，保留原纹理、UV 和无光照取色语义；修复后的最新 Play 画面已检查，原紫蓝背景及完成后的空杆正常。URP 自动完成 9→11 资产版本迁移；原 HDR 关闭、MSAA 1、renderScale 1、阴影关闭等配置保留。完整原版逐帧画面对照仍未完成。
- 真机方向配置已按原 PlayerSettings 恢复为 Portrait（序列化值 0），不再沿用新工程 AutoRotation 默认值。Android ARM64 IL2CPP 本地包已构建并校验；Android 真机运行和 iOS 包仍未验证。

- 已从原场景恢复加载界面、三个 Canvas 根结构、原 Logo/背景/进度条/标记图、TMP 字体回退链和描边材质。组件映射到官方 UGUI/TMP，补齐官方 TMP Essential Resources。加载动画保持每秒 0.5、上限 0.9、结束信号后 0.5 秒 OutQuad 到 1、再停留 0.2 秒关闭；标记按 750 单位行程，百分比保留 F0 取整语义并缓存字符串避免逐帧分配。
- 启动顺序改为先显示加载界面，发送完成信号后下一帧激活 GameScene，在加载界面完全关闭前阻止棋盘输入。当前本地初始化没有服务器/SDK 等待，未添加虚假等待或奖励结果。十二组回归通过，并在真实竖屏 Play 模式抓取当前完整加载画面，随后自动完成首关。首次加载和重复周期验证通过；重入并发关闭请求的原多 Tween 行为仍需随后续全生命周期调用链核对。

- 已接入原 SparkEffect/DoneEffect Prefab、粒子模块和必要材质/贴图，保持 Unity 内置粒子实现。落点火花挂在螺母 Root，下落回调触发，1.5 秒缩放时间后回池；完成彩色特效在杆帽峰值挂到原 DoneEffect 节点，2 秒后销毁，按原 12 色表着色并保留 CenterGlow/MagicDust 原色。资源按路径延迟加载，粒子引用预配置在 Prefab；高频火花复用，低频整杆完成保留原实例化方式。
- 粒子配置回归及真实 Play 验证通过：原首关一枚搬移触发一次火花和一次完成彩色效果，定时清理、暂停时间、火花复用及旋转复位已验证。棋盘清理同步回收附着临时特效。最新运行画面已检查；尚未完成原版和目标逐帧粒子画面对照，也未验证真机表现。三个待转换的骨骼动画、声音、主界面与后续生命周期缺口不因此视为完成。
- 已恢复原 `Resources/Tables/tables` 加密表包及实际 PasswordDeriveBytes/AES→GZip→二进制容器读取链路。根据全局无反射要求，使用只读取记录、字段和引用的数据解析器，保留原数据格式，不调用 BinaryFormatter 或解析 CLR 类型。四张表的明文字节 SHA-256 与独立提取结果一致：地区配置、关卡阶段、支付渠道配置、文案；支付仅恢复数据，未实现或调用 SDK。
- 已恢复 51 条关卡阶段记录及原 GetLevelInfo/ShowLevel 规则：内部第 4/5/6 关共用显示第 4 关，回合分别为 1/2/3；末条内部关卡 51 对应显示关卡 30；越界按原当前玩家关卡计算，保留默认回合字段。真实 GameScene 初始化读取该表并提供显示关卡数据。十四组回归通过；主面板视觉和实际交互仍待接入，46 条国家记录和 187 条文案的数据恢复不代表地区 AB/GM 或多语言 UI 已完成。
- 已恢复原文案选择逻辑：支持原 15 个显式语言代码，其他代码取 zh，空字符串/null 再回退 en；代码匹配保持大小写敏感，空白字符串不被擅自当作缺失。保留原 String.Format 参数、缺少参数异常、缺失 ID 返回数字和首条重复 ID 语义。使用显式字段读取，不反射查找语言字段；文案在表初始化时缓存，不逐帧解析 JSON。
- 已恢复 MainPanel 中关卡牌及其原父级层级，原背景精灵、蓝色描边材质、TMP 字体、自适应字号 18–40、尺寸和坐标保留。内部关卡小于 2 隐藏；隐藏关卡活动时隐藏并保留上次文字；普通状态 x=0，提现进度显示时 x=374，保留 y=-248。由原阶段表计算显示关卡，并使用文案 ID 2 格式化。
- 关卡牌接入当轮的 MainPanel.prefab 仅包含上述关卡牌和父级；其余余额、工具栏、奖励及事件节点仍待恢复。启动时从 Resources 实例化到原 UICanvas，接入当前显式关卡配置；语言暂取配置 en，隐藏关卡/提现进度状态的实际系统消费者未接通。十五组回归与真实 Play 验证通过。最新 `main-level-component-preview.png` 是首关完成画面上单独预览显示关卡 4 的组件验证，不能作为完整第 4 关流程或全画面对照依据。
- 已接入原 BGM、Select、Move1/2/3 以及后续按钮所需 Click 音频，文件和导入元数据与当前参考工程逐字节一致。BGM 启动在加载流程首次 yield 后；非空螺杆的 Select 位于后续操作门控之前，成功搬移播放按实际枚数选择的 Move 音效。未把整关完成音错误接到单杆完成回调。
- 音频由场景预配置 AudioMgr/BGM 音源、AudioVoice Prefab 和 ScriptableObject 驱动，音频通过 Resources 按需加载并缓存。遵循全局性能规范，将原版逐次 new GameObject/AddComponent/Destroy 的短音效改为 Prefab 对象池；这是明确的实现差异，保留原 AudioSource 参数、播放和结束判断。BGM 保留 DecompressOnLoad，Click 保留 CompressedInMemory；BGM 126.0146 秒、11000 Hz 双声道，若按 float PCM 估算为约 10.58 MiB，非实测总内存。
- 原声音开关语义已实现：关闭 Pause BGM、阻止新音效，不切断已经播放的短音效；开启调用 Play BGM。延迟使用缩放时间并在到期时读取开关。真实 Play 已验证 BGM/短音效 PCM 输出、开关、禁用时不加载新片段、延迟、重叠播放和池复用。十六组完整回归与真实首关 Play 通过，首关两次 Select、一次 Move1 及既有动画/粒子链均验证通过。设置面板按钮、UserLocalData 保存和其他生命周期声音尚未接入，当前默认值仍是显式配置。

- 已恢复主界面右下角 Replay 入口与原 ReplayPanel 完整 9 对象层级、2 个标准 Button、字体/精灵/尺寸、原 4 条表文案。恢复场景原 EventSystem/StandaloneInputModule，官方 UGUI 负责真实射线和点击；没有用玩法射线替代 UI Button。当前主界面已包含关卡牌与重开入口，设置、工具、余额等其他节点仍待恢复。
- 原 ReplayPanel 的 CloseBtn 和 ReplayBtn 指向同一个 Button。按源 InitLssPanel 的 Close→Continue→Replay 顺序替换绑定，最后保留实际重开回调；Continue 仅关闭。确认后立即清空当前棋盘并清理选中、搬移动画、杆帽回调和附着特效，0.3 秒缩放时间后重新走现有关卡选择器，保留当前玩家关卡和种子状态，非首次入场使用 0.5 秒螺母初始化延迟。地区/服务器状态和持久存档仍未接入，不能把本地重开视为完整 InitLevel 生命周期。
- 原 UIScriptableObject 的面板/标题曲线原样迁入原生配置：主面板 0.25 秒，标题延迟 0.15 秒后运行 0.25 秒；关闭为 0.25 秒 InBack，标题独立继续。黑色背景 alpha=220/255。Button 按压线性缩到 0.85，按下 0.2 秒、松开 0.15 秒；保留原每个渲染帧使用 fixedDeltaTime 推进的行为。按压组件仅负责视觉，代码在标准 Button.onClick 绑定实际操作。
- 点击采用原 TopUICanvas 透明 Mask，短暂阻挡 UI 射线；当前分支使用原 0.2 秒缩放时间，回调之后播放 Click。关卡入场 StageStart 音频已补入原延迟位置，首次 1.5 秒，重开 0.5 秒。所有相关结构和参数在 Prefab/Scene/配置中预置，没有运行时创建静态 UI、添加第三方 Tween 或 SDK。

- 十七组回归、真实 UGUI 射线/指针派发重开测试及音频回归通过：透明 Mask 命中、重复开关面板、取消保留棋盘/选中、重开清理及延迟恢复、单次 StageStart 均验证。最新 `replay-panel.png` 已检查；未完成原版与目标全画面的逐帧对照，不将该截图视为完整 1:1 的证明。

- 已恢复 UserLocalData 的 62 个顶层字段和原存档 key `UserLocalData`。字段按源类型显式读取/写入，不通过反射查找；原构造默认值 Level=1、LevelId=1、RevokeCount=5、IsAudio/IsVibrate=true 放入轻量配置，其他值保持零/null，未编造用户标识、名字、收益或服务器回复。三个嵌套用户/服务器/支付文档仅作为原 JSON 对象保留，尚未实现其完整类型与业务语义。
- 场景预配置 OriginalUserSession，从 Unity PlayerPrefs 读取共享玩家状态；音频开关改为读写其中 IsAudio，删除独立 DefaultEnabled 状态。原音频按钮语义仍为仅改内存；没有另造音频存档 key 或每次切换自动存档。重开实际接入原 InitLevel 的 LuckyScrewDoneCount=0、PassLevelTime=0，以及 UserLocalData.Init 仅清零 CurrentLevelAddScrewCount 的规则。
- 已实现原 SaveData 外层写入边界：无玩家数据不写；有当前棋盘时由调用方提供原格式 JSON 替换 LevelInfo；无当前棋盘时保留原字符串；整体序列化后 SetString 并 Save。**当前玩法自动保存与断点续局尚未接入**，避免在未恢复完整 LevelInfo 序列化关系前改写棋盘。GameScene 仍使用显式关卡配置，读取存档中的 Level 不代表已恢复该关的棋盘。
- 十八组回归验证通过，包含所有 62 字段的合成样本往返、64 位整数精度、遗漏字段默认值、重复/大小写字段顺序、非法输入不重置为新用户、嵌套 JSON 和 LevelInfo 字符串保留。真实 Play 以可恢复测试存档验证原 key 读取、启动静音、共享状态、无自动写入及显式整份保存/重读；原 PlayerPrefs 已恢复，备份文件已清理。重开真实 Play 也验证上述三项本关计数清零及既有面板/棋盘链无回归。该合成样本验证不等于已完成原版真机存档导入与续局对照。

- 已恢复原 `LevelInfo` 棋盘快照结构：螺杆 ID/索引、独立容量、固定槽位坐标、空槽、螺母类型/颜色/V、遮罩与嵌套对象的独立可见性、锁定状态，以及来源/目标/数量三字段操作记录。使用显式流式 JSON 读写，数字枚举不依赖名称，不序列化原版排除的 Unity 对象、选择标志、操作门控和计算属性。
- `OriginalLevelView.BindSaved` 使用现有原生 Prefab/对象池恢复棋盘，保持 0.5/1.5 秒入场延迟；完整的 `ScenePosGroup.Init(false) → ScrewInfo.Init` 调用链按存档行列定位，任一坐标缺失时才重算整个布局，同时保留固定螺母槽位。此前仅依据 `ScrewInfo.Init` 认定统一重排的实现已修正。捕获快照与后续重开数据独立，动画中的选择和门控不带入恢复。原始操作历史已从动画对象引用改为三字段记录。
- 棋盘快照验证通过：独立构造的原字段样例、null/缺省值、遮罩/锁定、忽略运行时字段、重复大小写字段顺序、无效输入拒绝、全部 1,482 次种子引用往返，以及实际 Prefab 恢复后选择/搬移/完成帽/释放重绑。完整 19 组回归通过。此轮提供快照与视图恢复能力，**尚未接通启动读档、操作后自动保存、已完成/空棋盘重建分支，也未验证原设备真实存档**；不能据此认定续玩生命周期已经复刻完成。

- 已将原 `ScrewInfo.Operator` 的实际保存调用点接入主场景：跨杆尝试先累计用户 `ScrewMoveCount`，失败回退或成功搬移后立即将棋盘 JSON 字符串写入 `UserLocalData.LevelInfo`，再保存整个用户对象并刷新 PlayerPrefs。单纯选中/取消不计数、不保存；动画帧不重复写入；重开保留累计次数，操作订阅随关卡视图复用保持单次调用。
- 完整 19 组回归及新增真实 Play 存档验证通过，覆盖失败尝试、成功搬移、动画门控未释放时已落盘、其他用户字段保留、重开后的累计计数和新棋盘历史。验证工具使用真实 PlayerPrefs，测试前备份、结束后恢复；临时备份只保留本机 Library，不提交。**启动选择存档、已完成/空棋盘重建、用户进度驱动选关以及其他生命周期保存点仍未接通。**

- 已修正续玩布局的前置差异：每排行列保留存档值，各排按实际杆数居中，相机使用存档最大列数。验证覆盖六列存档不被新关阈值拆成两排、对应的相机缩放，以及单根坐标缺失触发整盘重新排布。完整 19 组回归通过；启动自动读档尚未接入，不能将视图恢复验证视为完整续玩验证。

- 已补齐原 `LevelInfo.Init(true)` 从第 3 关开始追加锁定空杆的规则，阈值由场景配置保存，新关与重开共用。追加杆沿用最后一根杆 ID、索引加一、可用容量 0，空槽数取上一根杆容量，槽位坐标为 `(0,y,0)`；追加后整体参与原布局。恢复快照不重复追加，重开重新生成时也不累积。
- 20 组完整回归通过，新增真实 Prefab 验证覆盖第 1/2/3 关阈值、独立容量/槽位数、锁定两套外观、无螺母实例化、点击请求分支、快照恢复和重复新建。当前主场景仍使用显式会话关卡；玩家存档进度驱动、锁杆奖励消费和启动续玩仍待接通，SDK 未改动。

- 已接通实际启动续玩与用户进度选关。主场景和关卡标题读取 `UserLocalData.Level`，新关选择读取保存的 `LevelSeed/IsRandomLevelSeed`，选择结果更新用户 `LevelId/IsRandomLevelSeed`；移除会话配置中的临时 Level/Seed。空字符串新开，非空字符串按原棋盘快照恢复，均保留 0.3 秒重建延迟及 0.5/1.5 秒螺母入场延迟。
- 恢复后已完成或为空的棋盘按原调用链先初始化再清理，经第二次重建延迟回到当前玩家关卡的新棋盘；重置当前关卡计时/加杆次数，保留累计尝试数。未完成续玩保留计时/加杆次数，初始化均清除幸运完成计数。非空损坏 JSON、`null` 和空白文本不会触发静默新关兜底，也不会覆盖原存档。地区/服务端配置仍使用现有显式配置，未新增 SDK 行为。
- 21 组完整回归和真实 Play 续玩验证通过：实际 Start 恢复、进度标题、射线操作保存、重新加载场景后保留占用/历史/计数、继续操作完成、再次加载完成存档后新建关卡并恢复锁杆。静态场景验证另覆盖空棋盘、二次延迟、种子溢出、无效存档保留。验证前的本机 PlayerPrefs 已恢复；完整生命周期事件和原设备存档互通仍未验收。

- 已按当前 ARM64 证据实现初始化事件分支调度，包括记录引导优先、内部关卡 2/3 的奖励引导、死局检查顺序、引导编号 1/10/12/14、展示关卡 4 的提示、解锁及奖励横幅回调。记录引导完成按关闭、写标记、重入的顺序执行，不新增保存点或 SDK 行为。
- 22 组 Unity 回归通过，新增验证覆盖短路检查、初始化完成标记的先后顺序、横幅回调读取当前状态及首次进入提示。**调度器尚未绑定生产场景；首次引导等面板、死局和解锁消费者仍未恢复，不能视为玩家端引导已接通。** 详细证据、分支表和剩余接入工作见 `Docs/InitializationFlowAudit.md`。
- 已实现原 CheckDie / IsOnlyDontMove / IsCannotMove：保留四个同色螺母阈值、隐藏杆揭开后从头重新统计、每条掩层先改状态再请求动画再保存的顺序，以及死局检查的整组容量规则。关卡视图提供显式检查入口，实际场景连接已有整用户存档路径，揭开不增加操作次数。
- 原生隐藏对象按配置在 1 秒非缩放时间后关闭，回收取消旧计时；原 posui 动画请求已接入现有接口，**骨骼动画尚未恢复**。23 组完整回归与真实 Play 暂停验证通过，覆盖实际 PlayerPrefs 中间保存状态、延迟显隐、重复检查和回收取消。验证前存档已恢复。初始化调度仍待面板接入，未提前插入会跳过更高优先级引导的自动死局检查；失败弹窗及后续分支尚未完成。详见 `Docs/DeadlockAudit.md`。
- 已恢复 TXRecordGuidePanel 原 Prefab 的静态层级、锚点排版、字体材质、两套粒子与标准 Start Button，代码绑定点击。按钮保留 2 秒隐藏和 0.5 秒 OutBack 出现，关闭先于完成回调；已结束的缩放停止逐帧写入。五个 Get 标记按原 Punch 参数烘焙为 Unity AnimationCurve，无 DOTween 程序集。
- 按当前原包恢复九种货币贴图与原国家代码别名。按钮流光原导出是占位 Shader，已核实当前 GLES 程序及混合状态，改为原生 Unity Shader；未使用旧截图作视觉依据。24 组完整回归与实际 Play 面板渲染/射线点击验证通过，最新画面为 Library/ValidationCaptures/record-guide.png，已检查无丢失或品红资源，验证前存档已恢复。
- **记录引导组件仍运行于独立验证入口，尚未绑定完整启动调度。** 后续奖励横幅、新手/评估引导及生命周期回调仍待恢复；不能将当前截图视为原设备视觉对照通过，也不能视为完整玩家引导已完成。详见 `Docs/RecordGuideAudit.md`，SDK 未改动。
## 仍未完成

- 主界面其余区域、胜负后续分支、引导、奖励、地区初始化及其他生命周期保存点仍未完成。当前已可执行核心排序、重开、操作存档和启动续玩；原服务器/SDK 等待链没有被伪造。这些局部主链通过不能代替完整游戏生命周期验收。
- 原函数体已在逆向资料目录生成，仍需逐方法验证、移植和回归。解码、加载之外的规则不因“导出了函数体”而视为已实现。
- 原场景和带字段 Prefab 在独立参考工程内；未将其中 DummyDll、SDK、Spine、DOTween 当作正式实现导入。
- 地区 AB/GM、US 收益广告默认路径、完整视觉/动画、Editor 与设备一致性尚未验证。
- SDK 未改动；当前新工程没有原有 SDK 实现可继承。

- 已恢复奖励阶段 1–7 的原判定顺序、阶段二引导条件、奖励状态文案和地区货币格式。通过显式字段读取使用原存档奖励文档，不生成服务器收益配置、不改写未知字段。25 组 Unity 回归通过，含边界优先级、负数文案参数、地区精度与缓存行为。详见 RewardProgressAudit.md。**收益横幅及初始化动作宿主仍未接入主场景，地区 AB/GM 与完整生命周期尚未完成，不能认定完整 1:1。**
- 已将 TargetReward 收益横幅的原子层级、图片与 TMP 配置恢复到 MainPanel，并接入启动时的原 Init 隐藏状态。原生动画恢复 0.5 秒进入、1 秒停留、0.3 秒世界坐标飞行和独立缩放，保留回调先于顶部提示/进度、移动回调重置后缩放仍写入的顺序；文字接入七阶段规则和首次完成登录条件的保存请求。26 组回归及真实 Play 暂停/恢复检查通过，已检查当前生成的横幅截图。详见 TargetRewardBannerAudit.md。**顶部 GoldItem、TX_JD 和正式初始化宿主仍未接通，测试回调不代表完整主链完成。**
- 已恢复 MainPanel/Top/TX_JD 的原层级、填充条、三处文字、图片与三秒旋转光晕。实现原关卡门槛、收益/每日通关/登录/用户等级进度，以及收益引导前的替换显示；保留 IsShow 与 activeSelf 的独立语义。27 组回归和真实 Play 的横幅→进度条连接、暂停恢复、隐藏期间光晕相位验证通过，已检查最新进度条截图。详见 RewardProgressViewAudit.md。**正式 Top 刷新与初始化宿主、GoldItem 及后续引导仍未接通，验证数据不是正式收益配置。**
- 已恢复 GoldItem 原子层级、标准 Button、国家金额图标、185 号本地化标签、收益提示和原延迟缩放/浮字淡出动画；保留普通刷新只更新金额、带提示刷新才更新主界面相关提示，以及请求回调后才打开 18 号面板的顺序。28 组回归与真实 Play 的实际 GoldItem 横幅落点/提示/进度条连接、按钮命中、暂停及浮字清理通过，已检查两张最新阶段截图。详见 GoldItemAudit.md。**GoldItem 当前作为独立 Prefab 验证，正式 Top 初始化和国家/收益提供者尚未接通；SDK 请求只保留边界，未调用服务或发放收益。**
完整 1:1 状态仍为 **false**。开发继续优先主链与视觉。

## 验证入口

使用指定 Unity 执行 `-batchmode -nographics -projectPath <工程> -executeMethod NutSort.Validation.OriginalContentValidation.Run -logFile <日志>`。

日志必须出现 `NUT_CONTENT_VALIDATION_PASS`，且无编译错误或验证异常；不要仅依据启动 Unity 的 PowerShell 退出码判断，因为 Unity.exe 可提前返回进程启动结果。

期望清单由原加密数据在独立 PowerShell/.NET 解密后生成，Unity 验证实际 Resources 加载路径。测试代码仅在 Editor 验证程序集；游戏加载器没有 Editor 专用分支。

- 已按当前 ARM64 调用补齐金币独立阶段规则：使用 bear_zs_list、LoginDayCoin 和实际关卡大于 4 的展示条件，保留门槛 float 比较与文案 double 差额计算的区别、原文案编号及未钳制差值。数量格式补齐 F2、三地区整数截断，并与现金格式共用同一格式器实例的文化缓存。完整服务端 JSON 保持原样；没有伪造奖励数据或新增 SDK 行为。金币 Prefab/动画/点击分支及生产 MainPanelTop 尚待恢复，详见 Docs/CoinProgressAudit.md。
- 本轮 29 组完整 Unity 回归通过，覆盖金币独立阶段、精度边界、零换算率和现金/金币文化缓存联动；测试存档已恢复。此次未变更视觉资源，不作新的画面一致性结论。

- 已从当前 MainPanel 原 Prefab 恢复 CoinItem 的 8 个层级、33 个序列化对象、钻石图标、Cash 标准 Button、提示背景、字体材质及提示往返动画。普通刷新先写数量和提示，再应用整个入口的展示条件；阶段达到 5 时隐藏提示并保留旧文案。点击等待奖励信息回调后进入原面板编号 19，SDK 请求仍保持边界，不伪造奖励。
- 已恢复 0.6 秒延迟、12 次 OutSine/Yoyo 缩放、独立金币数值格式以及两秒上浮淡出的奖励表现。30 组完整 Unity 回归与实际 Play 射线点击、延迟回调、暂停恢复、副本清理通过；最新 coin-item.png / coin-item-gain.png 已检查，测试存档已恢复。生产 Top 及面板 19 尚未接通；整个入口停用时与原全局动画调度的差异仍待统一修复。详见 Docs/CoinItemAudit.md。
- 已恢复原 HideLevel 完整 38 个层级、143 个序列化对象、五个 Get 标记动画、原字体材质及两套布局。按原顺序实现节点进度、第四/第五节点轮数和子轮次刷新，保留前期 1～5 与后期 5/10/15/20/目标关卡的分支、整条隐藏后的后续刷新、边界显隐和已完成时保留进度值。
- 子轮次从原模板复制，正常流程复用四个并扩展到五个，当前圆点按原 OutQuad/Yoyo 闪烁。31 组完整 Unity 回归及实际 Play 验证通过，最新 hidden-level-rounds.png 和 hidden-level-milestones.png 已检查，测试存档已恢复。完整生产 Top 接入、全局动画调度差异、GM 逆向跳关时的子轮次缩减行为仍待处理；SDK 未改动。详见 Docs/HiddenLevelAudit.md。
- 已在原启动场景对象上接入 Unity 原生 UI 动画调度，修复现金、金币、奖励横幅和循环提示被隐藏/停用后停止更新的差异。有限动画按需登记、完成后退出，对象销毁后取消；保留缩放时间、登记快照和回调内移除顺序，无 Editor 分支或第三方程序集。
- 32 组回归及真实 Play 验证通过：隐藏现金与停用金币仍产生并清理奖励副本，隐藏横幅执行回调并保留原最终缩放顺序，隐藏标记摆动/光圈/提示位移继续更新，统一暂停和销毁取消正常。测试存档已恢复。本轮未改视觉资产；逐条动画跨组件交错、完整主流程及地区 AB/GM 仍未验收。详见 Docs/UIAnimationAudit.md。
- 已恢复 PMD 滚动提示数据层：显式读取原字段，按原顺序解析金额区间、标记现金项并合并提示项，保留对象复用、重复初始化、随机采样次数和原生非常规选择条件。缺失数据通过调用方提供的请求入口返回 null，不新增 SDK 请求实现或伪造提示数据。
- 33 组完整 Unity 回归通过，覆盖选择边界、请求回调时序、空/错误数据、区域数字解析及原 JSON 保留；测试存档已恢复。本轮尚未恢复 PMDBullet 滚动预制体和 PMDItem 文本图标，正式 Top 接入仍未完成。已确认其高宽比 2.1 门槛、实际第 4 关计时和双次数据选择，详见 Docs/MarqueeDataAudit.md。
- 已恢复 PMDItem 文本分支：使用调用方捕获的关卡，前两关读取原奖励列表金额，后续按现金标记选择 35/153 号文案，保留名称、随机金额和货币格式调用顺序。34 组完整 Unity 回归通过，测试存档已恢复。名称生成器、滚动预制体和正式 Top 仍待接入；SDK 未变更，未作完整视觉一致性结论。详见 Docs/MarqueeTextAudit.md。

- 已恢复滚动提示名称生成器并接入文本组件默认入口：原 A-Z 数组、五次整数随机调用、四种排列和当前区域小写行为均保留。35 组完整 Unity 回归通过，验证恢复随机状态、区域与测试存档。滚动预制体、图标和生产 Top 仍待接入，SDK 未改动。详见 Docs/MarqueeNameAudit.md。

- 已将原 pay.json 的 46 国渠道表接入运行时，恢复空参数读取当前国家、区域大写匹配、首项命中及原随机渠道选择；保留空项返回空字符串与空列表失败的区别。36 组完整 Unity 回归通过，测试存档已恢复。此为滚动提示图标显示依赖，尚未接通显示预制体及正式 Top；无支付或 SDK 调用。详见 Docs/PayChannelsAudit.md。

- 已恢复 PMDItem 原三层结构、14 个序列化对象、字体/ContentSizeFitter/背景与渠道图标资源，按当前 GLES 程序恢复圆形裁切 Shader。文本、图标选择和 0.01 秒延迟宽度回调按原顺序连接，重复初始化保留各自回调，缺图保留旧图。37 组回归通过，最新 marquee-item.png 已检查。滚动发射器、主对象协程绑定、正式 Top 和真机对照仍未完成；SDK 未改动。详见 Docs/MarqueeItemAudit.md。

- 已恢复 PMDBullet 原四层结构及发射器：高宽比 2.1 门槛、第 4 关计时、初始 5 秒、交替随机间隔、双次数据选择、原多余随机采样、上下轨道和 12–15 秒线性移动。忙碌时复制原模板、完成后复用，隐藏对象仍由全局动画调度移动。38 组回归通过。正式 Top、主对象宽度协程、实际 Play 时序与真机对照仍待验证；SDK 未改动。详见 Docs/MarqueeLauncherAudit.md。

- 已恢复主游戏对象上的 WaitForSeconds 延迟回调入口，并在实际 Play 中连接 PMDBullet/PMDItem 验证真实 Update 发射和宽度布局。暂停、隐藏后移动/回调继续、停用发射器后不新增提示及终点复用状态均通过；最新 marquee-play.png 已检查。38 组完整回归通过，测试存档已恢复。正式 Top/provider 接线与完整生命周期仍未完成；SDK 未改动。详见 Docs/MarqueePlayAudit.md。

- 已恢复 Top 初始化/刷新调度，连接真实组件验证 Gold 初始化内的交叉进度刷新、随后隐藏但保留 IsShow，以及金额/阶段进度/普通关卡标签刷新顺序。明确保留 Refresh 不主动 Show 收益进度条的原行为。39 组完整回归通过，测试存档恢复。调度尚未绑定生产启动，正式 Top 组装、GM 和生命周期 Show 仍待接入。详见 Docs/MainTopFlowAudit.md。

- 已恢复 Top 原 GM 标准按钮、文字/布局、按压反馈及原面板 3 路由，监听通过代码绑定，按调用方测试标记显隐，点击门槛通过后先打开面板再播放音效；SDK 振动跳过。接入 Top 联动验证后 39 组回归通过。GM 面板和地区 AB 切换尚未实现；Top 的 PlayerGoldGetHint 动态分支仍待恢复，不能作为静态节点跳过。详见 Docs/GMButtonAudit.md。

- 已恢复 PlayerGoldGetHint 的 UTC 整秒推送调度、-1 禁用状态、LSSUPT 随机间隔，以及面板/缺失数据时原定时间加 3 秒的重试规则；成功展示前先排下次推送，原配置保持不变。40 组完整回归通过，测试存档已恢复。其预制体、文本/粒子/展示动画和实际 Update 接线仍待完成，正式 Top 尚未完整组装；SDK 未改动。详见 Docs/PlayerGoldHintScheduleAudit.md。

- 已恢复 PlayerGoldGetHint 普通推送/自身收益两套名称和金额文案，保留当前关卡读取、95/96/97/152 号推送文案、94 号自身称呼及自身传入金额，不混用弹幕文本。41 组回归通过，原配置保持不变，测试存档恢复。其图标、预制体、展示动画与完整 Top 接入仍待完成；SDK 未改动。详见 Docs/PlayerGoldHintTextAudit.md。

- 已恢复收益提示渠道元数据查询、UserLssInfo.GetType 存档索引和原图标路径，区分普通推送缺图保留旧图与通用随机/自身收益直接赋值的行为。42 组回归通过，测试存档恢复。收益提示预制体/动画及正式 Top 仍待接入，SDK 未改动。详见 Docs/HintIconsAudit.md。

- 已恢复 PlayerGoldGetHint 原六对象预制体、背景/字体/礼花及三条展示入口，接入已核对的 UTC 调度、文案和渠道图标；普通/自身收益 3 秒停留后 OutQuad 退出，回调展示使用 Y 轴 OutBack 入场、0.5 秒停留和线性退出。43 组完整回归及真实 Play 验证通过，最新画面已检查，测试存档恢复。正式 Top/provider 接线、完整生命周期与地区 AB/GM 仍待完成；SDK 未改动。详见 Docs/PlayerGoldHintViewAudit.md。

- 已按原顺序组装完整 MainPanelTop：8 个子模块、77 个对象，全部 Transform 配置对照一致。统一绑定真实用户/格式化/渠道/点击门槛/请求回调/提示数据，保留收益提示由 MainPanel 单独初始化的归属。44 组回归及实际 Play 联动通过，早期/后期/提示覆盖的最新画面均已检查，存档恢复。普通启动的配置数据就绪链、MainPanel/Bottom/交换操作接入及地区 AB/GM 切换仍待完成；SDK 未改动。详见 Docs/MainTopViewAudit.md。

- 已恢复底部撤回/交换/加螺丝的原五对象预制体、数量/加号和灰态规则，原配置上限 LSSLSMAC 默认 12；变灰只影响活动 Image，不改 TMP 材质与按钮交互。45 组回归及真实 Play 通过，灰态/恢复彩色最新画面已检查，测试存档恢复。道具点击/消耗/奖励分支和完整 Bottom/MainPanel 启动接入仍待完成；SDK 未改动。详见 Docs/ToolItemDisplayAudit.md。

- 已恢复三种道具的代码按钮监听与原点击分支：撤回先执行操作再交道具管理器扣 1，无历史提示 3、无库存打开面板 4；交换只进入状态，加螺丝直接交核心管理器。原按压反馈已配置在预制体，46 组回归通过，测试存档恢复。核心执行/库存持久化/获取面板及完整 Bottom 接线仍待完成，SDK 未改动。详见 Docs/ToolItemControllerAudit.md。

- 已恢复完整 Bottom 的 20 对象预制体及原初始化/刷新调度，保留画面顺序与加螺丝/撤回/交换列表顺序的区别；设置路由 5、重玩路由 17，局部刷新仅首个匹配项。47 组回归及实际 Play 通过，两种最新画面已检查，存档恢复。正式 MainPanel/启动数据接线、面板功能和核心道具执行仍未完成，SDK 未改动。详见 Docs/MainBottomAudit.md。

- 已组装完整 MainPanel 的 105 对象预制体，所有 Transform 与原导出一致；恢复父级本地化/Top/Bottom/横幅/提示初始化顺序及交换遮罩请求 false、0.5 秒的回调，普通刷新不重置覆盖层。48 组回归与实际 Play 通过，完整布局和遮罩最新画面已检查，存档恢复。正常启动的数据就绪/provider 接线、核心道具执行和地区 AB/GM 仍待完成，SDK 未改动。详见 Docs/MainPanelAudit.md。

- 已恢复 ItemMgr 实际库存变更：按 IsMore 选择五取偶整数数量，不钳制负数，先 SaveData 再按参数刷新整个 Bottom；现金/金币转交原金额入口。实际按钮经控制器→道具管理器→存储序列化/写入/保存→Bottom 的顺序验证通过，共 49 组回归，测试存档恢复。核心撤回/加螺丝/交换操作、正常启动服务接线和地区 AB/GM 仍待完成，SDK 未改动。详见 Docs/ItemManagerAudit.md。

- 已恢复核心撤回管理调度：空历史直接返回，成功按对象删除首个匹配并刷新撤回灰态，拒绝仍检查死局；保留外层按钮在记录返回 false 后仍扣道具的原行为。50 组回归通过，测试存档恢复。记录级螺母反向移动/动画及正常启动服务接线仍待恢复，尚非完整棋盘撤回；SDK 未改动。详见 Docs/RevokeFlowAudit.md。
