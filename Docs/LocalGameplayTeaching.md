# Default local teaching composition

The default scene now instantiates the restored NewbieGuidePanel prefab and binds OriginalTeachingFlow. NewbieGuidePanel.SetTeach (0x9E2F70) is the source: level one enables the screw-operation override, shows Teach, hides the guide hand/hollow mask, fills markers inclusively through LevelSeed, and selects text 70/71/72 for the first three seeds and 151 for later seeds. The existing source new-level-mode close decision remains in OriginalTeachingFlow.

LocalGameplayController.Teaching binds this teaching-only path after the original board becomes ready and refreshes it when the native tutorial transition advances the seed. It participates in the existing modal predicate. World screw input passes through the original IsCanOperatorScrew override while the teaching panel is registered; UI still uses the source standard Buttons and counted click mask. Native final-teaching move completion closes the panel immediately. As in the source, closing alone does not clear the operation override; entry to level two or later resets it.

The prefab's hierarchy, marker sprites, text positions, font, nut illustration and interaction structure are retained. Source texts 70/71/72 remain unchanged. In the explicitly approved local SDK-skipped composition, the source withdrawal badge (text 1) reads the serialized caption Local, and text 151 is replaced by the serialized local progression message. These two explicit text substitutions avoid promising unavailable withdrawal functionality. Original label initialization remains unchanged for other callers. No reward guide branches, fabricated account state, SDK responses or currency grants are introduced.

Validation uses the actual default scene. LocalGameplayPlayValidation solves all four teaching seeds and levels two/three via world ray input, dispatches teaching UI clicks through EventSystem, checks source text and marker sprites, final-teaching closure, later-level override reset, local next/duplicate guards, persisted settled and partial boards, failure/retry and native replay. Library/local-teaching-play.log records NUT_LOCAL_GAMEPLAY_PLAY_PASS. That full progression run preceded the final badge-caption-only edit.

LocalTeachingViewPlayValidation checks the final composition using explicit starting-seed fixtures for all four stages, configured local badge/final text, absence of visible withdrawal claims, real world selection under the teaching override, and normal level-two entry. Library/local-teaching-view-play.log records NUT_LOCAL_TEACHING_VIEW_PASS. All four current Library/local-teaching-0..3-current.png captures were inspected after the caption edit. Preference fixtures restore the original user data. OriginalReplayMaskPlayValidation starts at level two so its focused replay test is not intercepted by the newly connected first-level teaching panel.

Full source-device visual equivalence, reward-dependent guides and the complete game lifecycle remain unverified or incomplete. Existing scene teardown pool diagnostics are not resolved by this change.

Final regression: Library/local-teaching-content-validation.log completes with 164 PASS markers and NUT_CONTENT_VALIDATION_PASS (1476 resources / 1474 boards). Library/local-teaching-replay-mask-play.log contains NUT_REPLAY_MASK_PLAY_VALIDATION_PASS for the level-two replay fixture. No compiler or validation exception was reported in these runs; the preference backup was restored.
